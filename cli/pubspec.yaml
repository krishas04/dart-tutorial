name: cli
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo
publish_to: none

environment:
  sdk: ^3.9.2

# Add regular dependencies here.
dependencies:
  path: ^1.9.0
  http: ^1.1.0  #The ^ symbol allows compatible versions to be used.
  command_runner:
    path: ../command_runner # Points to your local command_runner package

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
